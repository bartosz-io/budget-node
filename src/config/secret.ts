export const secret = {
  github: {
    clientID: '<place `clientID` here>',
    clientSecret: '<place `clientSecret` here>'
  },
  google: {
    clientID: '<place `clientID` here>',
    clientSecret: '<place `clientSecret` here>'
  }
}